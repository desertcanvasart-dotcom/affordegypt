node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
# dotenv files — never commit real env values
.env
.env.*
!.env.example
.local/
uploads/
.claude/

# Local prerender hydration diagnostic scripts (kept on disk, not committed)
hydration-check.mjs
dom-diff.mjs
