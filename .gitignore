node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
# Ignore every .env variant EXCEPT the placeholder template. The old rules
# only covered .env/.env.local/.env.*.local, which left .env.production and
# .env.production.save one `git add .` away from being published.
.env
.env.*
!.env.example
.local/
uploads/
.claude/
data/

# Local prerender hydration diagnostic scripts (kept on disk, not committed)
hydration-check.mjs
dom-diff.mjs

# Local DB backups (point-in-time dumps; never commit)
backups/
